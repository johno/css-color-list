var assert = require('assert');
var cssColorList = require('..');

describe('css-color-list', function() {

  it('should do something awesome', function() {
    assert.equal(cssColorList(), true);
  });
});
